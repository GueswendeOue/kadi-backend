"use strict";

const {
  sendText,
  sendTypingIndicator,
  sendButtons,
  sendList,
  getMediaInfo,
  downloadMediaToBuffer,
  uploadMediaBuffer,
  sendDocument,
  sendFlow,
} = require("./whatsappApi");

module.exports = {
  sendText,
  sendTypingIndicator,
  sendButtons,
  sendList,
  getMediaInfo,
  downloadMediaToBuffer,
  uploadMediaBuffer,
  sendDocument,
  sendFlow,
};
